import React from 'react'
import { Paper } from '@material-ui/core';
const RightPain = ({ styles }) => {
    return (

        <Paper style={styles.paper}>
            Slider
        </Paper>

    );
}

export default RightPain;